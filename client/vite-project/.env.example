# ─── REQUIRED IN PRODUCTION (Vercel) ─────────────────────────────────────────
# Set this to your Render backend URL in the Vercel project settings.
# Leave unset for local development — Vite proxies /api → http://localhost:5000
VITE_API_BASE=https://folioo-dxty.onrender.com/api
